// Jest setup file for utility tests
// This is a minimal setup that doesn't require database connections

// Increase timeout for tests
jest.setTimeout(30000);

// Add any global test setup here
beforeAll(async () => {
  // Add any setup needed for utility tests
});

// Cleanup after tests
afterAll(async () => {
  // Add any cleanup needed for utility tests
}); 